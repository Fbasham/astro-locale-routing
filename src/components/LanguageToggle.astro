---
let { lang } = Astro.params;
let { pathname } = Astro.url;
let path = pathname.replace(/\/(en|fr)\//, "");
---

<a href=`/${lang==='en' ? 'fr' :'en'}/${path}` class="underline"
  >{lang === "en" ? "Français" : "English"}</a
>
